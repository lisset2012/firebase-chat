function GoogleAuthBtn(){
    const btn = document.createElement('button');

    container.id = 'google-auth-btn';
    container.classList.add('google-auth-btn');

    btn.innerHTML = `
   chat screen`;
    return container;
}